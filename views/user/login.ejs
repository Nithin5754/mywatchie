<!DOCTYPE html>
<html lang="en">
  
  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="dist/output.css">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.15/dist/tailwind.min.css" rel="stylesheet">

</head>
<body class="bg-gray-100">

  <section class="flex flex-col h-screen items-center">
    <div class="bg-indigo-600 hidden lg:block w-full h-screen">
      <img src="<%= randomBannerImage %>" alt="Random Banner Image" class="w-full h-full object-cover">
    </div>

    <div class="style_091  w-full md:max-w-md lg:max-w-full md:mx-auto md:mx-0 md:w-1/2 xl:w-1/3 h-screen px-6 lg:px-16 xl:px-12
        flex items-center justify-center">

      <div class="w-full h-100">

        <h1 class="text-xl md:text-2xl font-bold leading-tight mt-12 font-091 text-center">Log in to your account</h1>

        <form class="mt-6" action="/login" method="post">
          <div>
            <label class="block text-gray-700">Email Address</label>
            <input type="email" name="lEmail" id="" value="<%= locals.prevEnter %>" placeholder="Enter Email Address"
              class="w-full px-4 py-3 rounded-lg bg-gray-200 mt-2 border focus:border-blue-500 focus:bg-white focus:outline-none"
              autofocus autocomplete>
          </div>

          <div class="mt-4">
            <label class="block text-gray-700">Password</label>
            <input type="password" name="lPassword" id="" placeholder="Enter Password" minlength="3"
              class="w-full px-4 py-3 rounded-lg bg-gray-200 mt-2 border focus:border-blue-500
                  focus:bg-white focus:outline-none">
          </div>

          <div class="text-right mt-2">
            <a href="/forgotPassWordDisplay"
              class="text-sm font-semibold text-gray-700 hover:text-blue-700 focus:text-blue-700">Forgot
              Password?</a>
          </div>

          <p class="text-red-900 font-medium text-center"><%= locals.msg %></p>

          <button type="submit"
            class="w-full block bg-indigo-500 hover:bg-indigo-400 focus:bg-indigo-400 text-white font-semibold rounded-lg
            px-4 py-3 mt-6">Log In</button>
        </form>

        <hr class="my-6 border-gray-300 w-full">

        <p class="mt-8 text-center">Need an account? <a href="/signup"
            class="text-blue-500 hover:text-blue-700 font-semibold">Create an account</a></p>

      </div>
    </div>
  </section>

  <style>
    /* Add responsive styles for small screens (sm) */
    @media (max-width: 640px) {
      .style_091 {
        margin-top: 5rem;
        width: 90vw;
        right: auto;
        left: 50%;
        transform: translateX(-50%);
      }

      .bg-indigo-600 {
        display: none;
      }
    }

    .style_091 {
      background-color: blue;
      margin-top: 8rem;
      position: absolute;
      height: 70vh;
      background-color: rgba(109, 142, 146, 0.759);
      border-radius: 20px;
      backdrop-filter: blur(4px);
      box-shadow: 0 0 20px white;
    }

    .font-091 {
      color: rgb(41, 40, 40);
      text-align: center;
    }
  </style>
</body>


</html>